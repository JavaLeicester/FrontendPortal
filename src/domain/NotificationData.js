class NotificationData {
    constructor(styling, title, message) {
        this.styling = styling;
        this.title = title;
        this.message = message;
    }
}

export default NotificationData;
